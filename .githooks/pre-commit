#!/bin/sh
set -eu

if command -v pwsh >/dev/null 2>&1; then
  exec pwsh -NoProfile -File eng/privacy-check.ps1 -Staged
fi

exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File eng/privacy-check.ps1 -Staged
